#!/bin/sh

madd() {
    queue="$(mpc playlist)"
    if [ -z "$queue" ]; then
        mpc insert "$filename"
        mpc play
    else
        mpc insert "$filename"
        mpc next
    fi
}

filename=$(mpc listall | dmenu -i -l 30)
if [ -n "$filename" ]; then
    madd
fi

